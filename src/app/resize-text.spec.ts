import { ResizeText } from './resize-text';

describe('ResizeText', () => {
  it('should create an instance', () => {
    const directive = new ResizeText();
    expect(directive).toBeTruthy();
  });
});
